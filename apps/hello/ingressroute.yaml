apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hello
  namespace: hello-nginx
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`hello.local`)
      kind: Rule
      services:
        - name: hello
          port: 80
